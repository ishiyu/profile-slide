{
  "private": true,
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export": "slidev export",
    "pub": "slidev build --base profile-slide && cp ./404.html ./dist/404.html && node ./modifyPage.js && gh-pages -d dist"
  },
  "dependencies": {
    "@slidev/cli": "^0.6.3",
    "@slidev/theme-default": "^0.6.3",
    "@slidev/theme-seriph": "^0.6.3"
  },
  "name": "profile-slide",
  "devDependencies": {
    "playwright-chromium": "^1.19.2"
  }
}
